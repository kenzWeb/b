# 📚 Образовательная Платформа - Backend API

## Что это за проект?

Это **бэкенд** (серверная часть) веб-приложения для онлайн-образования. Проект позволяет:

- 👤 Регистрировать и авторизовать пользователей
- 📖 Просматривать каталог курсов
- 🛒 Покупать (записываться на) курсы
- 💳 Обрабатывать оплату через платёжную систему
- 📜 Проверять сертификаты об окончании курса

---

## 🗂 Структура файлов и папок

```
backend/
├── manage.py           # Главный файл для запуска проекта
├── db.sqlite3          # База данных (создаётся автоматически)
├── media/              # Папка для загруженных изображений курсов
├── config/             # Настройки проекта
│   ├── settings.py     # Главный файл настроек
│   ├── urls.py         # Главные маршруты (адреса) сайта
│   ├── wsgi.py         # Файл для запуска на сервере
│   └── asgi.py         # Файл для асинхронного запуска
├── users/              # Приложение для работы с пользователями
│   ├── models.py       # Модель пользователя (email + пароль)
│   └── admin.py        # Настройки админ-панели
├── courses/            # Приложение для курсов
│   ├── models.py       # Модели: Курс и Урок
│   └── admin.py        # Настройки админ-панели для курсов
├── students/           # Приложение для записей на курсы
│   ├── models.py       # Модель записи (Enrollment)
│   └── admin.py        # Настройки админ-панели
├── api/                # REST API (интерфейс для общения с сервером)
│   ├── views.py        # Логика обработки запросов
│   ├── urls.py         # Адреса API-методов
│   ├── serializers.py  # Преобразование данных в JSON
│   ├── authentication.py # Авторизация через токен
│   ├── exceptions.py   # Обработка ошибок
│   └── pagination.py   # Постраничный вывод
└── venv/               # Виртуальное окружение (папка с Python)
```

---

## 🧩 Описание моделей (данных)

### 👤 User (Пользователь)

| Поле     | Описание                                    |
| -------- | ------------------------------------------- |
| email    | Email пользователя (используется для входа) |
| password | Пароль                                      |
| username | Имя пользователя                            |

### 📚 Course (Курс)

| Поле        | Описание                                      |
| ----------- | --------------------------------------------- |
| name        | Название курса (макс. 30 символов)            |
| description | Описание (макс. 100 символов)                 |
| hours       | Продолжительность (макс. 10 часов)            |
| price       | Цена (минимум 100 рублей)                     |
| start_date  | Дата начала курса                             |
| end_date    | Дата окончания курса                          |
| img         | Обложка курса (только .jpg/.jpeg, макс. 2 МБ) |

### 📝 Lesson (Урок)

| Поле         | Описание                           |
| ------------ | ---------------------------------- |
| course       | К какому курсу относится           |
| name         | Название урока (макс. 50 символов) |
| text_content | Текстовое содержание               |
| video_link   | Ссылка на видео (super-tube.cc)    |
| hours        | Длительность (макс. 4 часа)        |

### 📋 Enrollment (Запись на курс)

| Поле               | Описание                       |
| ------------------ | ------------------------------ |
| user               | Какой пользователь записался   |
| course             | На какой курс                  |
| date               | Дата записи                    |
| status             | Статус: pending/success/failed |
| order_id           | ID заказа для оплаты           |
| certificate_number | Номер сертификата              |

---

## 🌐 API-методы (адреса)

| Метод | Адрес                    | Что делает                      |
| ----- | ------------------------ | ------------------------------- |
| POST  | `/api/registr`           | Регистрация нового пользователя |
| POST  | `/api/auth`              | Авторизация (получение токена)  |
| GET   | `/api/courses/`          | Список всех курсов              |
| GET   | `/api/courses/{id}/`     | Детали курса (список уроков)    |
| POST  | `/api/courses/{id}/buy/` | Купить (записаться на) курс     |
| GET   | `/api/orders`            | Мои заказы                      |
| GET   | `/api/orders/{id}`       | Отменить заказ                  |
| POST  | `/api/payment-webhook`   | Вебхук для платёжной системы    |
| POST  | `/api/check-sertificate` | Проверить сертификат            |

---

## 🚀 Как создать такой же проект с нуля

### Шаг 1: Установка Python

1. Скачай Python с [python.org](https://www.python.org/downloads/)
2. При установке **ОБЯЗАТЕЛЬНО** отметь галочку **"Add Python to PATH"**
3. Проверь установку в терминале:
   ```bash
   python --version
   ```
   Должно показать что-то типа `Python 3.11.x`

### Шаг 2: Создание папки проекта

```bash
# Создай папку и зайди в неё
mkdir backend
cd backend
```

### Шаг 3: Создание виртуального окружения

```bash
# Создаём изолированное окружение для проекта
python -m venv venv

# Активируем его:
# На macOS/Linux:
source venv/bin/activate

# На Windows:
venv\Scripts\activate
```

После активации в терминале появится `(venv)` в начале строки.

### Шаг 4: Установка Django и библиотек

```bash
# Устанавливаем Django и DRF
pip install django djangorestframework pillow

# Сохраняем список зависимостей
pip freeze > requirements.txt
```

### Шаг 5: Создание проекта Django

```bash
# Создаём проект с именем config
django-admin startproject config .

# Создаём приложения
python manage.py startapp users
python manage.py startapp courses
python manage.py startapp students
python manage.py startapp api
```

### Шаг 6: Настройка проекта

Открой файл `config/settings.py` и добавь в `INSTALLED_APPS`:

```python
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    # Сторонние библиотеки
    'rest_framework',
    'rest_framework.authtoken',
    # Наши приложения
    'users',
    'courses',
    'students',
    'api',
]
```

Также в конец файла добавь:

```python
AUTH_USER_MODEL = 'users.User'

MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'

REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': (
        'api.authentication.BearerTokenAuthentication',
    ),
    'DEFAULT_PAGINATION_CLASS': 'api.pagination.CustomPagination',
    'PAGE_SIZE': 5,
}
```

### Шаг 7: Создание моделей

Скопируй код моделей в соответствующие файлы:

- `users/models.py` — модель пользователя
- `courses/models.py` — модели курса и урока
- `students/models.py` — модель записи

### Шаг 8: Создание базы данных

```bash
# Создаём файлы миграций
python manage.py makemigrations

# Применяем миграции (создаём таблицы)
python manage.py migrate
```

### Шаг 9: Создание суперпользователя

```bash
python manage.py createsuperuser
```

Введи email, username и пароль.

### Шаг 10: Запуск сервера

```bash
python manage.py runserver
```

Сервер запустится на `http://127.0.0.1:8000/`

- Админ-панель: `http://127.0.0.1:8000/admin/`
- API: `http://127.0.0.1:8000/api/`

---

## 📝 Полезные команды

| Команда                            | Что делает             |
| ---------------------------------- | ---------------------- |
| `python manage.py runserver`       | Запустить сервер       |
| `python manage.py makemigrations`  | Создать миграции       |
| `python manage.py migrate`         | Применить миграции     |
| `python manage.py createsuperuser` | Создать админа         |
| `python manage.py shell`           | Открыть Python-консоль |
| `pip install -r requirements.txt`  | Установить зависимости |

---

## 🔧 Как запустить существующий проект

Если у тебя уже есть этот проект:

```bash
# 1. Зайди в папку проекта
cd backend

# 2. Активируй виртуальное окружение
source venv/bin/activate   # macOS/Linux
# или
venv\Scripts\activate      # Windows

# 3. Установи зависимости (если нужно)
pip install django djangorestframework pillow

# 4. Применить миграции (если нужно)
python manage.py migrate

# 5. Запусти сервер
python manage.py runserver
```

---

## 💡 Словарик терминов

| Термин                    | Простым языком                                               |
| ------------------------- | ------------------------------------------------------------ |
| **Django**                | Фреймворк (набор инструментов) для создания сайтов на Python |
| **REST API**              | Способ общения между программами через HTTP-запросы          |
| **Модель**                | Описание структуры данных (таблицы в базе данных)            |
| **Миграция**              | Инструкция для создания/изменения таблиц в базе              |
| **View (представление)**  | Код, который обрабатывает запросы                            |
| **Serializer**            | Преобразует данные Python в JSON и обратно                   |
| **Виртуальное окружение** | Изолированная папка с Python-библиотеками                    |
| **Токен**                 | Ключ для авторизации (вместо логина/пароля)                  |

---

## 🎓 Дополнительные ресурсы

- [Официальная документация Django](https://docs.djangoproject.com/ru/)
- [Django REST Framework](https://www.django-rest-framework.org/)
- [Python для начинающих](https://pythonworld.ru/)
